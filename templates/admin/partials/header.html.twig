{% if app.user is not null and is_granted('ROLE_ADMIN') %}
    {% set current = app.request.attributes.get("_route") %}
    <nav class="navbar navbar-expand-lg navbar-dark">
        <div class="container">
            <a class="navbar-brand" href="{{ path('admin_dashboard') }}">Deliveroo like</a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav ml-auto">
                    <li class="nav-item">
                        <a class="nav-link{% if current == 'admin_dashboard' %} active{% endif %}" href="{{ path('admin_dashboard') }}">Administration</a>
                    </li>
{#                    <li class="nav-item">#}
{#                        <a class="nav-link{% if current == 'dashboard-products' or current == 'product_create' or current == 'product_edit' %} active{% endif %}" href="{{ path('dashboard-products') }}">Produits</a>#}
{#                    </li>#}
{#                    <li class="nav-item">#}
{#                        <a class="nav-link{% if current == 'dashboard-blog' or current == 'create_post' or current == 'edit_post' %} active{% endif %}" href="{{ path('dashboard-blog') }}">Blog</a>#}
{#                    </li>#}
{#                    <li class="nav-item">#}
{#                        <a class="nav-link{% if current == 'dashboard-tags' or current == 'tag_create' %} active{% endif %}" href="{{ path('dashboard-tags') }}">Tags</a>#}
{#                    </li>#}
{#                    <li class="nav-item">#}
{#                        <a class="nav-link{% if current == 'dashboard-comments' %} active{% endif %}" href="{{ path('dashboard-comments') }}">Commentaires</a>#}
{#                    </li>#}
{#                    <li class="nav-item">#}
{#                        <a class="nav-link{% if current == 'dashboard-categories' or current == 'category_create' or current == 'category_edit' %} active{% endif %}" href="{{ path('dashboard-categories') }}">Catégories</a>#}
{#                    </li>#}
                    <li class="nav-item dropdown">
                        <a href="#" class="nav-link{% if current == 'admin_profil' or current == 'adminProfil_password' %} active{% endif %} dropdown-toggle" data-toggle="dropdown" id="accountDropdownLink">
                            {#                                <img src="{% if app.user.avatarurl is null %}{{ asset('avatars/user-default-64px.png') }}#}
                            {#                        {% else %}{{ asset('avatars/' ~ app.user.avatarurl) }}{% endif %}" alt="Avatar de {{ app.user.username }}" class="avatar-mini rounded-circle">#}
                            {{ app.user.firstname }}
                        </a>
                        <div class="dropdown-menu dropdown-menu-right" aria-labelledby="accountDropdownLink">
{#                            <a href="{{ path('admin_profil') }}" class="dropdown-item">Modifier mon profil</a>#}
{#                            <a href="{{ path('adminProfil_password') }}" class="dropdown-item">Modifier mon mot de passe</a>#}
                            <div class="dropdown-divider"></div>
                            <a href="{{ path('admin_deconnexion') }}" class="dropdown-item">Se déconnecter</a>
                        </div>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
{% else %}
    <div class="header-admin pb-5">
        <div class="container text-center">
            <a class="brand" href="accueil">Deliveroo_like</a>
            <h1 class="text-white mt-3">Connexion à l'administration</h1>
        </div>
    </div>
{% endif %}
{% for label, messages in app.flashes %}
    <div class="container text-center pt-5">
        <div class="alert alert-{{ label }}">
            {% for message in messages %}
                <p>{{ message | raw }}</p>
            {% endfor %}
        </div>
    </div>
{% endfor %}
